import { useStyles } from "../../Style/main_style"

const M= (props: any)=> {
    const classes= useStyles()
    return (
        <div className={classes.mainIndex}>

        </div>
    )
}

export default M